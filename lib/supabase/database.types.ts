// lib/supabase/database.types.ts

// ✅ Opção A (recomendada): cole aqui o conteúdo real gerado pelo Supabase CLI.
// `npm run supabase:generate-types` (no seu template) já gera um arquivo de types.

export type Database = any;
